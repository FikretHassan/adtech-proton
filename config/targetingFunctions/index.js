/**
 * Targeting Functions Index
 * Aggregates all custom targeting functions from individual files
 *
 * Add your own functions by:
 * 1. Create a new .js file in this folder (e.g., myFunction.js)
 * 2. Export a default function that returns a value
 * 3. Import and add it to the exports below
 * 4. Reference in targeting.json: { "source": "internal", "fn": "myFunction" }
 */

export default {
};
